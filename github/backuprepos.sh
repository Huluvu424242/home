#!/bin/sh

###
### Huluvus Backup
###
OWNER=Huluvu424242;

# umbenannte Projekte entfernen
rm -rf honey-speech-component.git
rm -rf slideshow-portal.git

# [0] clone 1jar-maven-plugin
REPO_NAME=1jar-maven-plugin;
git clone --mirror https://github.com/${OWNER}/${REPO_NAME}.git ${OWNER}/${REPO_NAME}.git
cd ${OWNER}/${REPO_NAME}.git;git remote update; cd ../..

# [1] clone ahnen.dsl
REPO_NAME=ahnen.dsl
git clone --mirror https://github.com/${OWNER}/${REPO_NAME}.git ${OWNER}/${REPO_NAME}.git
cd ${OWNER}/${REPO_NAME}.git;git remote update; cd ../..

# [2] clone algorithmen.example
REPO_NAME=algorithmen.example
git clone --mirror https://github.com/${OWNER}/${REPO_NAME}.git ${OWNER}/${REPO_NAME}.git
cd ${OWNER}/${REPO_NAME}.git;git remote update; cd ../..

# [3] clone angular-webcomponents.template
REPO_NAME=angular-webcomponents.template
git clone --mirror https://github.com/${OWNER}/${REPO_NAME}.git ${OWNER}/${REPO_NAME}.git
cd ${OWNER}/${REPO_NAME}.git;git remote update; cd ../..

# [4] clone annotation-processor-plugin.example
REPO_NAME=annotation-processor-plugin.example
git clone --mirror https://github.com/${OWNER}/${REPO_NAME}.git ${OWNER}/${REPO_NAME}.git
cd ${OWNER}/${REPO_NAME}.git;git remote update; cd ../..

# [5] clone annotation-processor-xtend.example
REPO_NAME=annotation-processor-xtend.example
git clone --mirror https://github.com/${OWNER}/${REPO_NAME}.git ${OWNER}/${REPO_NAME}.git
cd ${OWNER}/${REPO_NAME}.git;git remote update; cd ../..

# [6] clone ant-ube-task
REPO_NAME=ant-ube-task
git clone --mirror https://github.com/${OWNER}/${REPO_NAME}.git ${OWNER}/${REPO_NAME}.git
cd ${OWNER}/${REPO_NAME}.git;git remote update; cd ../..

# [7] clone anttask-githubmirror-userrepos
REPO_NAME=anttask-githubmirror-userrepos
git clone --mirror https://github.com/${OWNER}/${REPO_NAME}.git ${OWNER}/${REPO_NAME}.git
cd ${OWNER}/${REPO_NAME}.git;git remote update; cd ../..

# [8] clone awk
REPO_NAME=awk
git clone --mirror https://github.com/${OWNER}/${REPO_NAME}.git ${OWNER}/${REPO_NAME}.git
cd ${OWNER}/${REPO_NAME}.git;git remote update; cd ../..

# [9] clone base64-downloader
REPO_NAME=base64-downloader
git clone --mirror https://github.com/${OWNER}/${REPO_NAME}.git ${OWNER}/${REPO_NAME}.git
cd ${OWNER}/${REPO_NAME}.git;git remote update; cd ../..

# [10] clone blender.example
REPO_NAME=blender.example
git clone --mirror https://github.com/${OWNER}/${REPO_NAME}.git ${OWNER}/${REPO_NAME}.git
cd ${OWNER}/${REPO_NAME}.git;git remote update; cd ../..

# [11] clone bndtools.example
REPO_NAME=bndtools.example
git clone --mirror https://github.com/${OWNER}/${REPO_NAME}.git ${OWNER}/${REPO_NAME}.git
cd ${OWNER}/${REPO_NAME}.git;git remote update; cd ../..

# [12] clone books.example
REPO_NAME=books.example
git clone --mirror https://github.com/${OWNER}/${REPO_NAME}.git ${OWNER}/${REPO_NAME}.git
cd ${OWNER}/${REPO_NAME}.git;git remote update; cd ../..

# [13] clone BoostX
REPO_NAME=BoostX
git clone --mirror https://github.com/${OWNER}/${REPO_NAME}.git ${OWNER}/${REPO_NAME}.git
cd ${OWNER}/${REPO_NAME}.git;git remote update; cd ../..

# [14] clone BuildBoost
REPO_NAME=BuildBoost
git clone --mirror https://github.com/${OWNER}/${REPO_NAME}.git ${OWNER}/${REPO_NAME}.git
cd ${OWNER}/${REPO_NAME}.git;git remote update; cd ../..

# [15] clone BuildBoost.dev
REPO_NAME=BuildBoost.dev
git clone --mirror https://github.com/${OWNER}/${REPO_NAME}.git ${OWNER}/${REPO_NAME}.git
cd ${OWNER}/${REPO_NAME}.git;git remote update; cd ../..

# [16] clone buildboost.example
REPO_NAME=buildboost.example
git clone --mirror https://github.com/${OWNER}/${REPO_NAME}.git ${OWNER}/${REPO_NAME}.git
cd ${OWNER}/${REPO_NAME}.git;git remote update; cd ../..

# [17] clone buildr.example
REPO_NAME=buildr.example
git clone --mirror https://github.com/${OWNER}/${REPO_NAME}.git ${OWNER}/${REPO_NAME}.git
cd ${OWNER}/${REPO_NAME}.git;git remote update; cd ../..

# [18] clone bundles
REPO_NAME=bundles
git clone --mirror https://github.com/${OWNER}/${REPO_NAME}.git ${OWNER}/${REPO_NAME}.git
cd ${OWNER}/${REPO_NAME}.git;git remote update; cd ../..

# [19] clone cnf
REPO_NAME=cnf
git clone --mirror https://github.com/${OWNER}/${REPO_NAME}.git ${OWNER}/${REPO_NAME}.git
cd ${OWNER}/${REPO_NAME}.git;git remote update; cd ../..

# [20] clone codersdojo.katas
REPO_NAME=codersdojo.katas
git clone --mirror https://github.com/${OWNER}/${REPO_NAME}.git ${OWNER}/${REPO_NAME}.git
cd ${OWNER}/${REPO_NAME}.git;git remote update; cd ../..

# [21] clone com.github.funthomas424242.rades.evm
REPO_NAME=com.github.funthomas424242.rades.evm
git clone --mirror https://github.com/${OWNER}/${REPO_NAME}.git ${OWNER}/${REPO_NAME}.git
cd ${OWNER}/${REPO_NAME}.git;git remote update; cd ../..

# [22] clone com.github.funthomas424242.rades.evm.generator
REPO_NAME=com.github.funthomas424242.rades.evm.generator
git clone --mirror https://github.com/${OWNER}/${REPO_NAME}.git ${OWNER}/${REPO_NAME}.git
cd ${OWNER}/${REPO_NAME}.git;git remote update; cd ../..

# [23] clone CryptMan
REPO_NAME=CryptMan
git clone --mirror https://github.com/${OWNER}/${REPO_NAME}.git ${OWNER}/${REPO_NAME}.git
cd ${OWNER}/${REPO_NAME}.git;git remote update; cd ../..

# [24] clone cucumber-js-selenium-webdriver-example
REPO_NAME=cucumber-js-selenium-webdriver-example
git clone --mirror https://github.com/${OWNER}/${REPO_NAME}.git ${OWNER}/${REPO_NAME}.git
cd ${OWNER}/${REPO_NAME}.git;git remote update; cd ../..

# [25] clone cucumber.example
REPO_NAME=cucumber.example
git clone --mirror https://github.com/${OWNER}/${REPO_NAME}.git ${OWNER}/${REPO_NAME}.git
cd ${OWNER}/${REPO_NAME}.git;git remote update; cd ../..

# [26] clone desktop-url-navigator
REPO_NAME=desktop-url-navigator
git clone --mirror https://github.com/${OWNER}/${REPO_NAME}.git ${OWNER}/${REPO_NAME}.git
cd ${OWNER}/${REPO_NAME}.git;git remote update; cd ../..

# [27] clone development-environment.eclipse-project
REPO_NAME=development-environment.eclipse-project
git clone --mirror https://github.com/${OWNER}/${REPO_NAME}.git ${OWNER}/${REPO_NAME}.git
cd ${OWNER}/${REPO_NAME}.git;git remote update; cd ../..

# [28] clone devtools
REPO_NAME=devtools
git clone --mirror https://github.com/${OWNER}/${REPO_NAME}.git ${OWNER}/${REPO_NAME}.git
cd ${OWNER}/${REPO_NAME}.git;git remote update; cd ../..

# [29] clone devtools-preferences.eclipse-plugin
REPO_NAME=devtools-preferences.eclipse-plugin
git clone --mirror https://github.com/${OWNER}/${REPO_NAME}.git ${OWNER}/${REPO_NAME}.git
cd ${OWNER}/${REPO_NAME}.git;git remote update; cd ../..

# [30] clone nitrite-spring-boot-starter
REPO_NAME=nitrite-spring-boot-starter
git clone --mirror https://github.com/${OWNER}/${REPO_NAME}.git ${OWNER}/${REPO_NAME}.git
cd ${OWNER}/${REPO_NAME}.git;git remote update; cd ../..

# [31] clone jenkinsmonitor
REPO_NAME=jenkinsmonitor
git clone --mirror https://github.com/${OWNER}/${REPO_NAME}.git ${OWNER}/${REPO_NAME}.git
cd ${OWNER}/${REPO_NAME}.git;git remote update; cd ../..

# [32] clone jar-install-plugin
REPO_NAME=jar-install-plugin
git clone --mirror https://github.com/${OWNER}/${REPO_NAME}.git ${OWNER}/${REPO_NAME}.git
cd ${OWNER}/${REPO_NAME}.git;git remote update; cd ../..

# [33] clone mypocketmod
REPO_NAME=mypocketmod
git clone --mirror https://github.com/${OWNER}/${REPO_NAME}.git ${OWNER}/${REPO_NAME}.git
cd ${OWNER}/${REPO_NAME}.git;git remote update; cd ../..

# [34] clone free-images
REPO_NAME=free-images
git clone --mirror https://github.com/${OWNER}/${REPO_NAME}.git ${OWNER}/${REPO_NAME}.git
cd ${OWNER}/${REPO_NAME}.git;git remote update; cd ../..

# [35] clone rezeptbuch
REPO_NAME=rezeptbuch
git clone --mirror https://github.com/${OWNER}/${REPO_NAME}.git ${OWNER}/${REPO_NAME}.git
cd ${OWNER}/${REPO_NAME}.git;git remote update; cd ../..

# [36] clone org.gui4j
REPO_NAME=org.gui4j
git clone --mirror https://github.com/${OWNER}/${REPO_NAME}.git ${OWNER}/${REPO_NAME}.git
cd ${OWNER}/${REPO_NAME}.git;git remote update; cd ../..

# [37] clone rezeptsamnmlung
REPO_NAME=rezeptsammlung
git clone --mirror https://github.com/${OWNER}/${REPO_NAME}.git ${OWNER}/${REPO_NAME}.git
cd ${OWNER}/${REPO_NAME}.git;git remote update; cd ../..

# [38] clone webspeech.example
REPO_NAME=webspeech.example
git clone --mirror https://github.com/${OWNER}/${REPO_NAME}.git ${OWNER}/${REPO_NAME}.git
cd ${OWNER}/${REPO_NAME}.git;git remote update; cd ../..

# [39] clone junit.example
REPO_NAME=junit.example
git clone --mirror https://github.com/${OWNER}/${REPO_NAME}.git ${OWNER}/${REPO_NAME}.git
cd ${OWNER}/${REPO_NAME}.git;git remote update; cd ../..

# [40] clone plantuml-maven-plugin
REPO_NAME=plantuml-maven-plugin
git clone --mirror https://github.com/${OWNER}/${REPO_NAME}.git ${OWNER}/${REPO_NAME}.git
cd ${OWNER}/${REPO_NAME}.git;git remote update; cd ../..

# [41] clone honey-testcomponent
REPO_NAME=honey-testcomponent
git clone --mirror https://github.com/${OWNER}/${REPO_NAME}.git ${OWNER}/${REPO_NAME}.git
cd ${OWNER}/${REPO_NAME}.git;git remote update; cd ../..

# [42] clone liona-speech-input
REPO_NAME=liona-speech-input
git clone --mirror https://github.com/${OWNER}/${REPO_NAME}.git ${OWNER}/${REPO_NAME}.git
cd ${OWNER}/${REPO_NAME}.git;git remote update; cd ../..

# [43] clone liona-slideshow
REPO_NAME=liona-slideshow
git clone --mirror https://github.com/${OWNER}/${REPO_NAME}.git ${OWNER}/${REPO_NAME}.git
cd ${OWNER}/${REPO_NAME}.git;git remote update; cd ../..

# [44] clone liona-toggle-button
REPO_NAME=liona-toggle-button
git clone --mirror https://github.com/${OWNER}/${REPO_NAME}.git ${OWNER}/${REPO_NAME}.git
cd ${OWNER}/${REPO_NAME}.git;git remote update; cd ../..

# [45] clone diplomarbeit
REPO_NAME=diplomarbeit
git clone --mirror https://github.com/${OWNER}/${REPO_NAME}.git ${OWNER}/${REPO_NAME}.git
cd ${OWNER}/${REPO_NAME}.git;git remote update; cd ../..

# [46] clone home
REPO_NAME=home
git clone --mirror https://github.com/${OWNER}/${REPO_NAME}.git ${OWNER}/${REPO_NAME}.git
cd ${OWNER}/${REPO_NAME}.git;git remote update; cd ../..

# [47] clone job-dealer
REPO_NAME=job-dealer
git clone --mirror https://github.com/${OWNER}/${REPO_NAME}.git ${OWNER}/${REPO_NAME}.git
cd ${OWNER}/${REPO_NAME}.git;git remote update; cd ../..

# [48] clone jsdoc3-maven-plugin
REPO_NAME=jsdoc3-maven-plugin
git clone --mirror https://github.com/${OWNER}/${REPO_NAME}.git ${OWNER}/${REPO_NAME}.git
cd ${OWNER}/${REPO_NAME}.git;git remote update; cd ../..

# [49] clone rezepte
REPO_NAME=rezepte
git clone --mirror https://github.com/${OWNER}/${REPO_NAME}.git ${OWNER}/${REPO_NAME}.git
cd ${OWNER}/${REPO_NAME}.git;git remote update; cd ../..

# [50] clone mystorybook
REPO_NAME=mystorybook
git clone --mirror https://github.com/${OWNER}/${REPO_NAME}.git ${OWNER}/${REPO_NAME}.git
cd ${OWNER}/${REPO_NAME}.git;git remote update; cd ../..

# [51] clone foile-pile
REPO_NAME=foile-pile
git clone --mirror https://github.com/${OWNER}/${REPO_NAME}.git ${OWNER}/${REPO_NAME}.git
cd ${OWNER}/${REPO_NAME}.git;git remote update; cd ../..

# [52] clone heroku-container
REPO_NAME=heroku-container
git clone --mirror https://github.com/${OWNER}/${REPO_NAME}.git ${OWNER}/${REPO_NAME}.git
cd ${OWNER}/${REPO_NAME}.git;git remote update; cd ../..

# [53] clone open-login
REPO_NAME=open-login
git clone --mirror https://github.com/${OWNER}/${REPO_NAME}.git ${OWNER}/${REPO_NAME}.git
cd ${OWNER}/${REPO_NAME}.git;git remote update; cd ../..


# [54] clone jlogin
REPO_NAME=jlogin
git clone --mirror https://github.com/${OWNER}/${REPO_NAME}.git ${OWNER}/${REPO_NAME}.git
cd ${OWNER}/${REPO_NAME}.git;git remote update; cd ../..

# [55] clone honey-webcomponents
REPO_NAME=honey-webcomponents
git clone --mirror https://github.com/${OWNER}/${REPO_NAME}.git ${OWNER}/${REPO_NAME}.git
cd ${OWNER}/${REPO_NAME}.git;git remote update; cd ../..

# [56] clone honey-slideshow
REPO_NAME=honey-slideshow
git clone --mirror https://github.com/${OWNER}/${REPO_NAME}.git ${OWNER}/${REPO_NAME}.git
cd ${OWNER}/${REPO_NAME}.git;git remote update; cd ../..

# [57] clone honey-speech (backup because later renamed to honey-speaker)
REPO_NAME=honey-speech
git clone --mirror https://github.com/${OWNER}/${REPO_NAME}.git ${OWNER}/${REPO_NAME}.git
cd ${OWNER}/${REPO_NAME}.git;git remote update; cd ../..

# [58] clone honey-speaker
REPO_NAME=honey-speaker
git clone --mirror https://github.com/${OWNER}/${REPO_NAME}.git ${OWNER}/${REPO_NAME}.git
cd ${OWNER}/${REPO_NAME}.git;git remote update; cd ../..

# [59] clone honey-news
REPO_NAME=honey-news
git clone --mirror https://github.com/${OWNER}/${REPO_NAME}.git ${OWNER}/${REPO_NAME}.git
cd ${OWNER}/${REPO_NAME}.git;git remote update; cd ../..

# [59] clone wc-storybook
REPO_NAME=wc-storybook
git clone --mirror https://github.com/${OWNER}/${REPO_NAME}.git ${OWNER}/${REPO_NAME}.git
cd ${OWNER}/${REPO_NAME}.git;git remote update; cd ../..

# [60] clone liona-feeds
REPO_NAME=liona-feeds
git clone --mirror https://github.com/${OWNER}/${REPO_NAME}.git ${OWNER}/${REPO_NAME}.git
cd ${OWNER}/${REPO_NAME}.git;git remote update; cd ../..

# [61] clone license-check.example
REPO_NAME=license-check.example
git clone --mirror https://github.com/${OWNER}/${REPO_NAME}.git ${OWNER}/${REPO_NAME}.git
cd ${OWNER}/${REPO_NAME}.git;git remote update; cd ../..

# [62] clone vanilla-webcomponent.example
REPO_NAME=vanilla-webcomponent.example
git clone --mirror https://github.com/${OWNER}/${REPO_NAME}.git ${OWNER}/${REPO_NAME}.git
cd ${OWNER}/${REPO_NAME}.git;git remote update; cd ../..

# [63] clone honey-chucknorris-jokes
REPO_NAME=honey-chucknorris-jokes
git clone --mirror https://github.com/${OWNER}/${REPO_NAME}.git ${OWNER}/${REPO_NAME}.git
cd ${OWNER}/${REPO_NAME}.git;git remote update; cd ../..

# [64] clone honey-template
REPO_NAME=honey-template
git clone --mirror https://github.com/${OWNER}/${REPO_NAME}.git ${OWNER}/${REPO_NAME}.git
cd ${OWNER}/${REPO_NAME}.git;git remote update; cd ../..

# [65] clone honey-style-it
REPO_NAME=honey-style-it
git clone --mirror https://github.com/${OWNER}/${REPO_NAME}.git ${OWNER}/${REPO_NAME}.git
cd ${OWNER}/${REPO_NAME}.git;git remote update; cd ../..

# [66] clone honey-papercss-style
REPO_NAME=honey-papercss-style
git clone --mirror https://github.com/${OWNER}/${REPO_NAME}.git ${OWNER}/${REPO_NAME}.git
cd ${OWNER}/${REPO_NAME}.git;git remote update; cd ../..

# [67] clone honey-bulma-style
REPO_NAME=honey-bulma-style
git clone --mirror https://github.com/${OWNER}/${REPO_NAME}.git ${OWNER}/${REPO_NAME}.git
cd ${OWNER}/${REPO_NAME}.git;git remote update; cd ../..

# [68] clone honey-bootstrap-style
REPO_NAME=honey-bootstrap-style
git clone --mirror https://github.com/${OWNER}/${REPO_NAME}.git ${OWNER}/${REPO_NAME}.git
cd ${OWNER}/${REPO_NAME}.git;git remote update; cd ../..

# [69] clone honey-normalize-style
REPO_NAME=honey-normalize-style
git clone --mirror https://github.com/${OWNER}/${REPO_NAME}.git ${OWNER}/${REPO_NAME}.git
cd ${OWNER}/${REPO_NAME}.git;git remote update; cd ../..

# [70] clone open-books
REPO_NAME=open-books
git clone --mirror https://github.com/${OWNER}/${REPO_NAME}.git ${OWNER}/${REPO_NAME}.git
cd ${OWNER}/${REPO_NAME}.git;git remote update; cd ../..

# [71] clone hausarbeit
REPO_NAME=hausarbeit
git clone --mirror https://github.com/${OWNER}/${REPO_NAME}.git ${OWNER}/${REPO_NAME}.git
cd ${OWNER}/${REPO_NAME}.git;git remote update; cd ../..

# [72] clone liona-core
REPO_NAME=liona-core
git clone --mirror https://github.com/${OWNER}/${REPO_NAME}.git ${OWNER}/${REPO_NAME}.git
cd ${OWNER}/${REPO_NAME}.git;git remote update; cd ../..

# [73] clone jsf.example
REPO_NAME=jsf.example
git clone --mirror https://github.com/${OWNER}/${REPO_NAME}.git ${OWNER}/${REPO_NAME}.git
cd ${OWNER}/${REPO_NAME}.git;git remote update; cd ../..

# [74] clone stencil-component.example
REPO_NAME=stencil-component.example
git clone --mirror https://github.com/${OWNER}/${REPO_NAME}.git ${OWNER}/${REPO_NAME}.git
cd ${OWNER}/${REPO_NAME}.git;git remote update; cd ../..

# [75] clone midi.example
REPO_NAME=midi.example
git clone --mirror https://github.com/${OWNER}/${REPO_NAME}.git ${OWNER}/${REPO_NAME}.git
cd ${OWNER}/${REPO_NAME}.git;git remote update; cd ../..

# [76] clone LinkFree
REPO_NAME=LinkFree
git clone --mirror https://github.com/${OWNER}/${REPO_NAME}.git ${OWNER}/${REPO_NAME}.git
cd ${OWNER}/${REPO_NAME}.git;git remote update; cd ../..

# [77] clone fakeDB4REST
REPO_NAME=fakeDB4REST
git clone --mirror https://github.com/${OWNER}/${REPO_NAME}.git ${OWNER}/${REPO_NAME}.git
cd ${OWNER}/${REPO_NAME}.git;git remote update; cd ../..




###
### PIUGroup Backup
###
OWNER=PIUGroup;

# [001] clone Raspi-Garage
REPO_NAME=Raspi-Garage
git clone --mirror https://github.com/${OWNER}/${REPO_NAME}.git ${OWNER}/${REPO_NAME}.git
cd ${OWNER}/${REPO_NAME}.git;git remote update; cd ../..

# [002] clone JavaAlgorithmen
REPO_NAME=JavaAlgorithmen
git clone --mirror https://github.com/${OWNER}/${REPO_NAME}.git ${OWNER}/${REPO_NAME}.git
cd ${OWNER}/${REPO_NAME}.git;git remote update; cd ../..

# [003] clone automated-asciidoc-template
REPO_NAME=automated-asciidoc-template
git clone --mirror https://github.com/${OWNER}/${REPO_NAME}.git ${OWNER}/${REPO_NAME}.git
cd ${OWNER}/${REPO_NAME}.git;git remote update; cd ../..

# [004] clone piug-homepage
REPO_NAME=piug-homepage
git clone --mirror https://github.com/${OWNER}/${REPO_NAME}.git ${OWNER}/${REPO_NAME}.git
cd ${OWNER}/${REPO_NAME}.git;git remote update; cd ../..

# [005] clone fakeDB4REST
REPO_NAME=fakeDB4REST
git clone --mirror https://github.com/${OWNER}/${REPO_NAME}.git ${OWNER}/${REPO_NAME}.git
cd ${OWNER}/${REPO_NAME}.git;git remote update; cd ../..
